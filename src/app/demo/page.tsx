import { Box, Button, Title } from "@mantine/core";

const Demo = () => {
  return (
    <Box p={36}>
      <Title order={1}>デモページ</Title>
      <Button color="cyan">ボタン</Button>
    </Box>
  );
};

export default Demo;
